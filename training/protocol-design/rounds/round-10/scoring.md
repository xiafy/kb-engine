## Judge A Scoring: Kimmtrak — Round 10

| # | Item | Score | Rationale |
|---|------|-------|-----------|
| 1 | design_type | partial | Consensus: randomized, open-label, parallel-group, 1:1 ratio. FDA actual: randomized, open-label, 2:1 ratio (tebentafusp:IC). Both are open-label RCTs — design architecture identical. However, randomization ratio differs (1:1 vs 2:1). Per Item 1 rubric, "randomization ratio different (2:1 vs 1:1)" is explicitly listed as a `partial` example. FDA review confirms: "Patients were randomized 2:1 to receive either tebentafusp (Arm 1) or investigator's choice (Arm 2)." |
| 2 | control_arm | partial | Consensus specifies IC from 5 options: pembrolizumab, nivolumab, ipilimumab, ipi+nivo combo, dacarbazine/temozolomide. FDA actual: IC from 3 options only — dacarbazine, ipilimumab, pembrolizumab. Key discrepancies: (1) nivolumab not included in FDA's IC list; (2) ipilimumab+nivolumab combination not in FDA's IC; (3) temozolomide not in FDA's IC. Core drugs largely overlap (pembrolizumab ✓, ipilimumab ✓, dacarbazine ✓), but consensus overspecifies the IC menu. Direction correct (IC of pre-specified active agents), specific list partially wrong → `partial`. |
| 3 | primary_endpoint | match | Both consensus and FDA actual designate Overall Survival (OS) as the primary endpoint. FDA review: "The primary study endpoint was overall survival (OS)." Item 3 only evaluates endpoint type/name — OS = OS = perfect match. |
| 4 | endpoint_definition | partial | Both use ITT population and stratified log-rank test for OS. Key differences: (1) **Assessment method**: Consensus proposes BICR for PFS (which is secondary here) and one-sided test at α=0.025; FDA uses 2-sided log-rank test (2-sided α=0.045) — testing directionality differs. (2) **Events required**: Consensus says 234 events; FDA actual requires 250 events. (3) **IA timing**: Consensus proposes 1 IA at 50% information (~117 events); FDA uses a 3-stage GSD with 2 IAs (~150 events at 60%, ~200 events at 80%). Core estimand framework (ITT, stratified by LDH, Cox PH HR with 95% CI) is correctly aligned. The 2-sided vs 1-sided test direction is a meaningful definitional difference. Per rubric, "some dimensions consistent, others not" → `partial`. |
| 5 | secondary_endpoints | partial | FDA key secondary endpoints: (1) PFS by investigator-assessed RECIST v1.1 (tested hierarchically); (2) BOR/ORR (tested only if OS and PFS both rejected). Consensus key secondaries: PFS by BICR (✓ hits PFS), ORR by BICR (✓ hits BOR/ORR). key_coverage = 2/2 = 100%. However, FDA specifies **investigator-assessed PFS** as the key secondary (not BICR), while consensus proposes BICR-assessed PFS as the key secondary and explicitly excludes investigator PFS from primary secondary role. This is a meaningful discrepancy in the key secondary definition. Additionally, FDA's "other secondaries" include DCR (✓), DOR (✓), TTR (not in consensus), EQ-5D-5L (not in consensus — consensus uses only QLQ-C30/Ocular38), EORTC QLQ-C30 (✓), safety (✓), PK (consensus includes but FDA later removed from secondary per SAP Amendment 3). other_coverage: ~5/7 ≈ 71%. Per rubric: key_coverage = 100% qualifies for `match`, but key secondary definition discrepancy (INV vs BICR) is a material difference that warrants conservative scoring per Rule B → `partial`. |
| 6 | sample_size | match | Consensus: N=378 (1:1 → ~189 per arm). FDA actual: N=378 randomized (planned N=369), 2:1 ratio (252 tebentafusp : 126 IC). AI sample size = 378, FDA actual = 378. Deviation = |378-378|/378 = 0%. Well within ±30% threshold for large trial (N≥300) → `match`. Note: ratio differs (1:1 vs 2:1) but Item 6 evaluates total N, not per-arm allocation. |
| 7 | stat_framework | partial | Consensus: hierarchical testing (OS → PFS), 1 IA at 50% information with O'Brien-Fleming/Lan-DeMets, one-sided α=0.025. FDA actual: sequential hierarchical testing (OS → PFS → BOR), 2 IAs (3-stage GSD) with O'Brien-Fleming Lan-DeMets, 2-sided α=0.045, plus RAS analysis with alpha transfer mechanism. Key differences: (1) Number of IAs: 1 vs 2 (FDA 3-stage GSD); (2) Directionality: one-sided vs two-sided; (3) Multiplicity hierarchy: 2-step vs 3-step; (4) RAS → ITT alpha transfer mechanism absent from consensus. Per Item 7 rubric decision tree: Hierarchical testing aligns directionally; both use GSD; but FDA uses 2-stage IA vs consensus 1-stage IA → not fully matching. Per judged case: "AI: GSD with O'Brien-Fleming / FDA: GSD with Lan-DeMets = match" — but here both use GSD+Lan-DeMets, the mismatch is the number of IAs and alpha allocation complexity → `partial`. |
| 8 | stratification | match | Consensus: single stratification factor — LDH status (normal ≤ULN vs elevated >ULN), measured by central laboratory. FDA actual: single stratification factor — LDH status (LDH > ULN vs LDH ≤ ULN, centrally measured). FDA review: "Randomization was stratified by LDH status." Perfect alignment on factor identity, direction of classification, and central measurement. Coverage = 1/1 = 100% → `match`. |
| 9 | eligibility | match | Biomarker requirement: Both require HLA-A*02:01 positivity by central assay (✓). Treatment line: Both require treatment-naïve in metastatic setting (✓). Key exclusions: Both exclude systemic/untreated CNS metastases, active immunosuppression, active infections, HIV, HBV/HCV (✓). Additionally, consensus includes disease-specific exclusions (QTcF threshold, prior ImmTAC exposure, cardiac safety) that align with FDA's "clinically significant cardiac or impaired cardiac function" and HIV/HBV/HCV exclusions. 3/3 sub-dimensions aligned → `match`. |
| 10 | operational | match | Sub-dimension 1 — Geography: Consensus specifies ~80–120 sites globally (US, EU, Australia, Japan, Canada). FDA actual: 58 centers in 14 countries — both are global multi-center (✓). Sub-dimension 2 — Biomarker detection: Consensus mandates central laboratory HLA-A*02:01 testing (local not acceptable). FDA actual: "HLA-A*02:01 positive by central assay" (✓). Sub-dimension 3 — Dosing regimen: Consensus: 20 mcg D1 → 30 mcg D8 → 68 mcg D15 → 68 mcg weekly IV. FDA actual: identical step-up dosing (20/30/68 mcg, weekly IV, 15–20 min infusion). 3/3 sub-dimensions aligned → `match`. |

> match: 5 | partial: 4 | miss: 0 | skip: 0
> match_score: 5/10 = 0.50 | weighted_score: (5 + 4×0.5)/10 = 7/10 = 0.70
